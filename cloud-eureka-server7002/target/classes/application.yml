server:
  port: 7002


eureka:
  instance:
    hostname: eureka7002test.com #eureka服务端的实例名称
  client:
    #false表示不向注册中心注册自己
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001test.com:7001/eureka/

