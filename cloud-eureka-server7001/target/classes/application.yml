server:
  port: 7001


eureka:
  instance:
    hostname: eureka7001test.com #eureka服务端的实例名称
  client:
    #false表示不向注册中心注册自己
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7002test.com:7002/eureka/

